            foreach (DataGridViewRow row in dataGridView1.Rows)//sprijeći dupli unos
            {
                try
                {
                    string constring = "datasource=localhost;port=3306;username=root;password=pass123";
                    string upit = "select Rbr from poreznaura.ura ;";
                    MySqlConnection bazaspoj = new MySqlConnection(constring);
                    MySqlCommand bazazapovjed = new MySqlCommand(upit, bazaspoj);
                    bazaspoj.Open();
                    MySqlDataReader citaj = bazazapovjed.ExecuteReader();
                    while (citaj.Read())
                    {
                        int id = Convert.ToInt32(row.Cells[0].Value);
                        string red = row.Cells[1].Value.ToString();
                        if (red == citaj.GetString("Rbr"))
                        {
                            MessageBox.Show("Račun pod rednim brojem " + red +
                                ". je več u bazi i biti će preskočen.");
                            dataGridView1.Rows.RemoveAt(id - 1);
                        }
                        if (row.Cells[0].Value.ToString() == "") continue;
                    }
                    bazaspoj.Close();
                }
                catch (Exception ex)
                {
                    MessageBox.Show(ex.Message);
                }
            }

Direktan upit za izracun PDV-a za mjesec
string upit =
                "SET @ukpretp := (SELECT sum(ura.Ukupni_pretporez) as pretpor FROM ura WHERE ura.Datum_racuna BETWEEN '"+ datumOdBox +"' AND '"+ datumDoBox +"');" +
                "SET @ukpor := (SELECT sum(ira.pdv_uk) as porez FROM ira WHERE ira.datum_rn BETWEEN '"+ datumOdBox +"' AND '"+ datumDoBox +"');" +
                "SET @porosn5 := (SELECT sum(ira.osn5) FROM ira WHERE ira.datum_rn BETWEEN '"+ datumOdBox +"' AND '"+ datumDoBox +"');" +
                "SET @porosn13 := (SELECT sum(ira.osn13) FROM ira WHERE ira.datum_rn BETWEEN '"+ datumOdBox +"' AND '"+ datumDoBox +"');" +
                "SET @porosn25 := (SELECT sum(ira.osn25) FROM ira WHERE ira.datum_rn BETWEEN '"+ datumOdBox +"' AND '"+ datumDoBox +"');" +
                "SET @por5 := (SELECT sum(ira.pdv5) FROM ira WHERE ira.datum_rn BETWEEN '"+ datumOdBox +"' AND '"+ datumDoBox +"');" +
                "SET @por13 := (SELECT sum(ira.pdv13) FROM ira WHERE ira.datum_rn BETWEEN '"+ datumOdBox +"' AND '"+ datumDoBox +"');" +
                "SET @por25 := (SELECT sum(ira.pdv25) FROM ira WHERE ira.datum_rn BETWEEN '"+ datumOdBox +"' AND '"+ datumDoBox +"');" +
                "SET @porosn0:= (SELECT sum(ura.Porezna_osn0) FROM ura WHERE ura.Datum_racuna BETWEEN '" + datumOdBox + "' AND '" + datumDoBox + "');" +
                "SET @pretposn5 := (SELECT sum(ura.Porezna_osn5) FROM ura WHERE ura.Datum_racuna BETWEEN '" + datumOdBox +"' AND '"+ datumDoBox +"');" +
                "SET @pretposn13 := (SELECT sum(ura.Porezna_osn13) FROM ura WHERE ura.Datum_racuna BETWEEN '" + datumOdBox +"' AND '"+ datumDoBox +"');" +
                "SET @pretposn25 := (SELECT sum(ura.Porezna_osn25) FROM ura WHERE ura.Datum_racuna BETWEEN '" + datumOdBox +"' AND '"+ datumDoBox +"');" +
                "SET @pretpor5 := (SELECT sum(ura.por5) FROM ura WHERE ura.Datum_racuna BETWEEN '" + datumOdBox +"' AND '"+ datumDoBox +"');" +
                "SET @pretpor13 := (SELECT sum(ura.por13) FROM ura WHERE ura.Datum_racuna BETWEEN '" + datumOdBox +"' AND '"+ datumDoBox +"');" +
                "SET @pretpor25 := (SELECT sum(ura.por25) FROM ura WHERE ura.Datum_racuna BETWEEN '" + datumOdBox +"' AND '"+ datumDoBox +"');" +
                "SELECT @ukpretp as ukpretpor, @ukpor as ukpor, (@ukpor - @ukpretp) AS za_platit, @pretposn5 as osn5, " +
                "@pretposn13 as osn13, @pretposn25 as osn25,(@pretposn5+@pretposn13+@pretposn25) as osnPretPorUk, " +
                "@pretpor5 as ppor5, @pretpor13 as ppor13, @pretpor25 as ppor25, @porosn0 as porosn0, @porosn5 as porosn5, " +
                "@porosn13 as porosn13, @porosn25 as porosn25, (@porosn5+@porosn13+@porosn25) as ukporosn, " +
                "@por5 as por5, @por13 as por13, @por25 as por25; ";